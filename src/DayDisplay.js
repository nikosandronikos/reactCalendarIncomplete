import React from 'react';

const dayStrings = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];

// This is a class component, convert me to a functional component
class DayDisplay extends React.Component {
    render() {
        // Must display the day of the selected date.
        // The JS standard library includes a Date class that will be useful
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date
    }
}

export { DayDisplay };
